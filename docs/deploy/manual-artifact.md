# Manual artifact deployment

For the simplified Windows-to-VM deployment using Docker Compose and Caddy,
follow [`deploy/manual/README.md`](../../deploy/manual/README.md).

This path builds Linux images locally, transfers them as a checked Docker image
archive, and runs them without GitHub Actions or a container registry.
