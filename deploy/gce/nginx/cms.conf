# CMS admin + API + media
# Install to /etc/nginx/sites-available/tienda-magico-cms.conf
#
# Replace cms.example.com (or set via deploy.sh sed).
# TLS: certbot after DNS, or terminate at a GCP HTTPS LB.

upstream tm_cms {
    server 127.0.0.1:4000;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name cms.example.com;

    # Media / large Lexical payloads
    client_max_body_size 32m;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://tm_cms;
    }
}
