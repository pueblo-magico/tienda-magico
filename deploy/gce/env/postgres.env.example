# Copy to /etc/tienda-magico/postgres.env (created automatically by configure / db-up)
# chmod 640 — contains secrets
#
# Used by: docker compose -f deploy/gce/docker-compose.postgres.yml --env-file ...

POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD
POSTGRES_DB=tienda_magico_cms
