# VM deployment configuration. Copy to /etc/tienda-magico/deploy.env.
# Values are provided by the GitHub Environment / deployment setup, not source code.
STOREFRONT_IMAGE=us-central1-docker.pkg.dev/PROJECT_ID/tienda/storefront
CMS_IMAGE=us-central1-docker.pkg.dev/PROJECT_ID/tienda/cms

# This is written by the release activator. Do not set it manually.
TIENDA_IMAGE_TAG=
